# Base URL of the GeoLens backend REST API.
VITE_API_BASE_URL=http://localhost:8000

# "mock" uses the built-in mock API (src/api/mock) with no backend required.
# "real" talks to VITE_API_BASE_URL. Real integration lands once the
# documented API contract is available.
VITE_API_MODE=mock
